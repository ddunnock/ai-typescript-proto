[virtualenvs]
# Create virtual environments inside the project directory (.venv/)
in-project = true
create = true

[installer]
# Use parallel installation for faster dependency resolution
parallel = true
